# birdwatcher
Play with bird detection and recognition.

See [CLAUDE.md](CLAUDE.md) for design notes, tech stack, and hardware details.

```sh
just setup      # install deps and pre-commit hooks
just check      # ruff, mypy, pytest
uv run bird --help
```
